function add(a, b) {
  return a + b;
}

function divide(a, b) {
  return a / b;
}

function multiply(a, b) {
  return a * b;
}

function mod(a, b) {
  return a % b;
}

function exponent(a, b) {
  //   let expo = a
  //   if (b===0) {return 1}
  //   else if (b===1) {return a }
  //     else {
  //       for (let i=2; i<=b; i++) {
  //            expo *= a
  //            }
  //     }
  //
  //   return expo;

  return Math.pow(a, b);
}

function subt(a, b) {
  return a - b;
}
